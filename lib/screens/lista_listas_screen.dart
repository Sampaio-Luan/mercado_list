import 'package:flutter/material.dart';

class ListaListas extends StatelessWidget {
  const ListaListas({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.amber,
      child: const Text('Lista de Listas'),
    );
  }
}
